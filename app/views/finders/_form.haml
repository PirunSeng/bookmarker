= simple_form_for @finder do |f|
  - if @finder.errors.any?
    #error_explanation
      %h2
        = pluralize(@finder.errors.count, "error")
        prohibited this finder from being saved:
      %ul
        - @finder.errors.full_messages.each do |message|
          %li= message
  .form-group
    = f.input :name, input_html: { class: 'form-control' }
  .form-group
    = f.input :reference, input_html: { class: 'form-control' }
  .form-group
    = f.association :category, prompt: "Select Category", input_html: { class: 'form-control' }
  .form-group
    = f.submit class: 'btn btn-primary'
